# Beyond the Horizon - 详细开发指南

## 📋 项目概述

**Beyond the Horizon** 是一个关于视障人士导航体验的 Godot 4.5 游戏，通过模拟视障人士在街道上行走60秒的体验，提高公众对视障群体日常挑战的认识。

### 🎯 游戏目标
- 玩家扮演视障人士，在60秒内穿越多个街道场景
- 通过听觉提示和按键操作（A/S/D）避开障碍物
- 根据失误次数获得通行评级

### 👥 目标开发者
- 2位无Godot经验但有编程基础的开发者
- 支持分工协作，并行开发

---

## 🏗️ 项目结构总览

```
beyond-the-horizon/
├── assets/                 # 资源文件
│   ├── fonts/             # 字体文件
│   └── icons/             # 图标文件
├── audio/                 # 音频文件
│   ├── bgm/              # 背景音乐
│   └── sfx/              # 音效
│       ├── ambience/     # 环境音
│       ├── obstacles/    # 障碍音效
│       └── ui/           # UI音效
├── resources/            # 配置资源
├── scenes/               # 场景文件
│   ├── main/            # 主要场景
│   ├── prefabs/         # 预制体
│   └── ui/              # UI场景
└── scripts/             # 脚本文件
	├── core/            # 核心逻辑
	├── systems/         # 系统模块
	├── ui/              # UI脚本
	└── utils/           # 工具类
```

---

## 📅 9天开发计划

### 🗓️ Day 1: 搭建项目骨架

#### 🎯 目标
建立基础场景结构，能显示行人和3条轨道

#### 📁 关键文件
- `scenes/main/Main.tscn` - 游戏根场景
- `scenes/main/World.tscn` - 游戏世界场景
- `scenes/prefabs/Player.tscn` - 玩家角色
- `scripts/core/GameSession.gd` - 游戏会话管理

#### 🔧 具体任务
1. **创建主场景**
   - 新建场景，选择"2D Scene"
   - 重命名为"Main"，保存到`scenes/main/Main.tscn`
   - 添加子节点：
	 ```
	 Main (Node2D)
	 ├── World (Node2D)
	 ├── HUD (Control)
	 ├── AudioManager (Node)
	 └── GameSession (Node)
	 ```

2. **创建世界场景**
   - 新建场景，选择"2D Scene"
   - 添加背景色（深灰色）
   - 绘制3条白色轨道线（水平方向）
   - 保存到`scenes/main/World.tscn`

3. **创建玩家角色**
   - 新建场景，选择"2D Scene"
   - 添加Sprite2D节点，使用简单的圆形或人形图标
   - 位置设置在屏幕左侧中央
   - 保存到`scenes/prefabs/Player.tscn`

4. **基础脚本**
   - 创建`scripts/core/GameSession.gd`
   - 实现简单的启动逻辑
   - 在Main场景中附加脚本
#### ✅ 验收标准
- 能够运行游戏，看到玩家角色和3条轨道
- 无报错信息

---

### 🗓️ Day 2: 障碍物系统

#### 🎯 目标
实现障碍物从右向左移动的基本功能

#### 📁 关键文件
- `scenes/prefabs/Obstacle.tscn` - 障碍物预制体
- `scripts/core/ObstacleSpawner.gd` - 障碍物生成器

#### 🔧 具体任务
1. **创建障碍物预制体**
   - 新建场景，选择"2D Scene"
   - 添加Sprite2D，使用临时图标（彩色方块）
   - 添加简单移动脚本`Obstacle.gd`
   - 保存到`scenes/prefabs/Obstacle.tscn`

2. **创建障碍物生成器**
   - 创建`scripts/core/ObstacleSpawner.gd`
   - 实现定时生成逻辑

3. **集成到主场景**
   - 在World场景中添加ObstacleSpawner节点
   - 附加ObstacleSpawner脚本
   - 在Inspector中设置obstacle_scene为Obstacle.tscn

#### ✅ 验收标准
- 障碍物从右侧随机轨道生成，向左移动
- 生成间隔稳定（约2秒一个）

---

### 🗓️ Day 3: 输入处理系统

#### 🎯 目标
实现A/S/D按键响应，能够清除对应轨道的障碍物

#### 📁 关键文件
- `scripts/systems/InputHandler.gd` - 输入处理器
- `scripts/systems/MistakeTracker.gd` - 失误追踪器

#### 🔧 具体任务
1. **创建输入处理器**

2. **创建失误追踪器**

3. **设置输入映射**
   - 在Project Settings > Input Map中添加：
	 - `ui_left`: 物理按键 A
	 - `ui_down`: 物理按键 S  
	 - `ui_right`: 物理按键 D

4. **集成系统**
   - 在Main场景中添加InputHandler和MistakeTracker节点
   - 连接信号：
	 - InputHandler.obstacle_cleared → (显示清除效果)
	 - InputHandler.mistake_made → MistakeTracker.add_mistake
	 - MistakeTracker.game_over → GameSession._on_game_over

#### ✅ 验收标准
- 按A/S/D能清除对应轨道的障碍物
- 按错或轨道无障碍时计为失误
- 失误达到5次时触发游戏结束

---

### 🗓️ Day 4: 视觉效果系统

#### 🎯 目标
添加迷雾效果和粒子特效

#### 📁 关键文件
- `scenes/prefabs/ParticleClear.tscn` - 清除特效
- `scripts/systems/VisualEffects.gd` - 视觉效果管理

#### 🔧 具体任务
1. **添加迷雾效果**
   - 在Main场景中添加Light2D节点
   - 设置为仅照亮玩家周围区域
   - 调整光源范围和颜色（暖黄色）
   - 添加CanvasLayer实现全局暗化

2. **创建清除特效**
   - 新建场景，选择"2D Scene"
   - 添加GPUParticles2D节点
   - 配置粒子参数：
	 - 粒子材质：白色圆点
	 - 发射方向：圆形扩散
	 - 生命周期：0.5秒
   - 保存到`scenes/prefabs/ParticleClear.tscn`

3. **视觉效果管理器**
   ```gdscript
   # scripts/systems/VisualEffects.gd
   extends Node
   @export var particle_scene: PackedScene
   
   func show_clear_effect(pos):
	   var particles = particle_scene.instantiate()
	   particles.position = pos
	   get_tree().current_scene.add_child(particles)
	   await particles.finished
	   particles.queue_free()
   ```

4. **连接特效**
   - 修改InputHandler，清除障碍物时触发特效
   - 添加闪屏效果（短暂的颜色覆盖）

#### ✅ 验收标准
- 屏幕大部分区域变暗，只有玩家周围明亮
- 清除障碍物时有粒子特效
- 视觉反馈清晰明显

---

### 🗓️ Day 5: 音频系统

#### 🎯 目标
实现完整的音效系统，包括环境音、障碍音和UI音

#### 📁 关键文件
- `scripts/utils/AudioManager.gd` - 音频管理器
- 音频资源文件

#### 🔧 具体任务
1. **创建音频管理器**
   ```gdscript
   # scripts/utils/AudioManager.gd
   extends Node
   var audio_players = {}
   
   func _ready():
	   # 预加载所有音效
	   load_sounds()
   
   func load_sounds():
	   # 这里后续会加载实际的音频文件
	   pass
   
   func play_sound(sound_name, position = Vector2.ZERO):
	   if audio_players.has(sound_name):
		   var player = audio_players[sound_name]
		   if position != Vector2.ZERO:
			   player.position = position
		   player.play()
   ```

2. **准备音效文件**
   - 环境音：鸟鸣声、街道环境音
   - 障碍音：自行车铃声、汽车声等
   - UI音：按钮点击、成功/失败提示音
   - 特殊音：风铃声（连续成功奖励）

3. **集成音效触发**
   - 障碍物生成时播放预警音
   - 障碍物进入视野时播放识别音
   - 清除障碍物时播放成功音
   - 失误时播放错误音

#### ✅ 验收标准
- 游戏运行时有持续的背景环境音
- 各种游戏事件都有对应的音效反馈
- 音效播放时机正确

---

### 🗓️ Day 6: 场景管理系统

#### 🎯 目标
实现多场景切换和动态难度调整

#### 📁 关键文件
- `resources/scenes_config.json` - 场景配置
- `scripts/core/SceneManager.gd` - 场景管理器

#### 🔧 具体任务
1. **创建场景配置文件**
   ```json
   [
	 {
	   "name": "安静小区",
	   "duration": 15,
	   "base_speed": 100,
	   "density": 0.6,
	   "obstacles": ["bike", "trash"]
	 },
	 {
	   "name": "早市街道", 
	   "duration": 20,
	   "base_speed": 150,
	   "density": 0.8,
	   "obstacles": ["bike", "cart", "stand"]
	 },
	 {
	   "name": "商业区",
	   "duration": 15,
	   "base_speed": 200,
	   "density": 1.0,
	   "obstacles": ["car", "bike", "cone"]
	 },
	 {
	   "name": "学校门口",
	   "duration": 10,
	   "base_speed": 120,
	   "density": 0.7,
	   "obstacles": ["bike", "child"]
	 }
   ]
   ```

2. **实现场景管理器**
   ```gdscript
   # scripts/core/SceneManager.gd
   extends Node
   signal scene_changed(scene_name)
   signal all_scenes_completed()
   
   var current_scene_index = 0
   var scenes = []
   var scene_timer = 0
   
   func _ready():
	   load_scenes_config()
	   start_next_scene()
   
   func load_scenes_config():
	   var file = FileAccess.open("res://resources/scenes_config.json", FileAccess.READ)
	   var json_text = file.get_as_text()
	   var json = JSON.new()
	   json.parse(json_text)
	   scenes = json.data
   
   func start_next_scene():
	   if current_scene_index >= scenes.size():
		   all_scenes_completed.emit()
		   return
	   
	   var scene = scenes[current_scene_index]
	   scene_changed.emit(scene.name)
	   scene_timer = scene.duration
	   current_scene_index += 1
   ```

3. **动态难度调整**
   - 根据当前场景调整障碍物生成速度
   - 修改障碍物移动速度
   - 调整生成间隔

#### ✅ 验收标准
- 游戏能够按照配置文件切换不同场景
- 每个场景有不同的难度和障碍物类型
- 场景名称正确显示在UI上

---

### 🗓️ Day 7: UI系统完善

#### 🎯 目标
完善游戏UI，包括计时器、失误计数和场景显示

#### 📁 关键文件
- `scenes/ui/HUD.tscn` - 游戏界面
- `scripts/ui/HUDController.gd` - UI控制器

#### 🔧 具体任务
1. **创建HUD界面**
   - 添加以下UI元素：
	 - Label: 时间显示（左上角）
	 - Label: 失误计数（右上角）
	 - Label: 当前场景名称（顶部中央）
	 - ProgressBar: 进度条（可选）

2. **实现UI控制器**
   ```gdscript
   # scripts/ui/HUDController.gd
   extends Control
   @onready var time_label = $TimeLabel
   @onready var mistakes_label = $MistakesLabel
   @onready var scene_label = $SceneLabel
   
   func update_time(seconds):
	   time_label.text = "时间: %d秒" % seconds
   
   func update_mistakes(count):
	   mistakes_label.text = "失误: %d" % count
   
   func update_scene_name(name):
	   scene_label.text = name
	   # 2秒后淡出
	   await get_tree().create_timer(2.0).timeout
	   var tween = create_tween()
	   tween.tween_property(scene_label, "modulate:a", 0, 1.0)
   ```

3. **创建挑战选择界面**
   - 主菜单场景
   - 挑战模式选择（速通/零失误/纯听觉/默认）
   - 开始按钮

#### ✅ 验收标准
- UI显示实时更新的游戏信息
- 场景名称能够正确显示和淡出
- 挑战选择界面功能正常

---

### 🗓️ Day 8: 结局反馈系统

#### 🎯 目标
实现游戏结束时的评级系统和个性化反馈

#### 📁 关键文件
- `scripts/systems/VictoryEvaluator.gd` - 评级计算器
- `scenes/ui/ResultPanel.tscn` - 结果面板

#### 🔧 具体任务
1. **实现评级计算器**
   ```gdscript
   # scripts/systems/VictoryEvaluator.gd
   extends Node
   enum Rating {SMOOTH, HARD, FAILED}
   
   func evaluate(mistakes, last_scene_difficulty):
	   if mistakes == 0:
		   return Rating.SMOOTH
	   elif mistakes <= 3:
		   return Rating.HARD
	   else:
		   return Rating.FAILED
   
   func get_feedback_text(rating, mistakes):
	   var texts = {
		   Rating.SMOOTH: [
			   "他稳稳地走过了这条街，就像每天一样。",
               "一切都那么顺利，仿佛这条路走了千百遍。"
		   ],
		   Rating.HARD: [
			   "他在煎饼摊前绊了一下，但很快就站稳了。",
               "路上的车子有点多，但他还是小心地通过了。"
		   ],
		   Rating.FAILED: [
			   "今天的路似乎特别难走，需要更多练习。",
               "也许明天会是更好的一天。"
		   ]
	   }
	   return texts[rating].randi() % texts[rating].size()
   ```

2. **创建结果面板**
   - 显示评级图标（🟢/🟡/🔴）
   - 显示个性化文案
   - 显示游戏统计数据
   - 重新开始/返回主菜单按钮

3. **行动呼吁内容**
   - 显示真实的视障人士统计数据
   - 提供相关组织和资源链接
   - 鼓励用户了解更多信息

#### ✅ 验收标准
- 游戏结束时显示正确的评级
- 反馈文案与游戏表现匹配
- 统计数据准确显示

---

### 🗓️ Day 9: 测试与优化

#### 🎯 目标
完成最终测试、性能优化和打包发布

#### 🔧 具体任务
1. **功能测试**
   - 完整游戏流程测试
   - 边界情况测试
   - 音效和视觉同步性检查

2. **性能优化**
   - 检查内存使用
   - 优化粒子效果
   - 调整音频压缩

3. **打包发布**
   - 设置导出预设
   - 测试不同平台
   - 生成可执行文件

4. **文档完善**
   - 更新README文件
   - 创建操作说明
   - 准备发布材料

#### ✅ 验收标准
- 游戏运行稳定无崩溃
- 性能表现良好
- 成功打包为可执行文件

---

## 🎨 资源准备清单

### 🖼️ 图标资源
**下载地址**: [Game-Icons.net](https://game-icons.net/)

**必需图标**:
- `bicycle` - 自车
- `market-stand` - 市场摊位  
- `traffic-cone` - 施工锥
- `car` - 汽车
- `trash` - 垃圾堆
- `dog-poop` - 宠物粪便

**下载步骤**:
1. 访问 game-icons.net
2. 搜索上述关键词
3. 选择64x64 PNG格式下载
4. 保存到 `assets/icons/` 目录

### 🔊 音效资源
**推荐资源包**:

| 类型 | 资源包 | 下载链接 | 许可证 |
|------|--------|----------|--------|
| 界面音效 | Kenney Interface Sounds | https://kenney.nl/assets/interface-sounds | CC0 |
| UI音频 | Kenney UI Audio | https://kenney.nl/assets/ui-audio | CC0 |
| 环境音 | Freesound City Ambience | https://freesound.org/people/InspectorJ/packs/35608/ | CC BY 3.0 |
| 风铃声 | Freesound Wind Chime | https://freesound.org/people/klankbeeld/sounds/514953/ | CC0 |

**处理步骤**:
1. 下载所有音效文件
2. 使用Audacity统一音量（-3dB）
3. 转换为OGG格式
4. 按分类保存到对应目录

### 📝 字体资源
**思源黑体**:
- 下载地址: [GitHub Release](https://github.com/adobe-fonts/source-han-sans/releases)
- 选择 `OTF/Simplified Chinese` 版本
- 保存到 `assets/fonts/` 目录
- 在Godot中创建DynamicFont资源

---

## 👥 分工协作方案

### 开发者 A: 核心逻辑系统
**负责文件**:
- `scripts/core/` - 所有核心脚本
- `scripts/systems/` - 系统模块
- `resources/` - 配置文件
- 项目整体架构设计

**主要任务**:
- Day 1: GameSession, 基础架构
- Day 2: ObstacleSpawner, 障碍物逻辑
- Day 3: InputHandler, MistakeTracker
- Day 6: SceneManager, 场景切换
- Day 8: VictoryEvaluator, 评级系统
- Day 9: 性能优化, 打包

### 开发者 B: 界面与资源
**负责文件**:
- `scenes/` - 所有场景文件
- `scripts/ui/` - UI相关脚本
- `assets/` - 图标和字体
- `audio/` - 音频资源

**主要任务**:
- Day 1: 基础场景搭建, Player预制体
- Day 4: 视觉效果, 粒子系统
- Day 5: 音频系统, 音效集成
- Day 7: HUD界面, 挑战选择
- Day 8: ResultPanel, 结局UI
- Day 9: 资源整合, 测试

### 协作流程
1. **每日同步**: 每天结束前合并代码，确保集成
2. **版本控制**: 使用Git管理代码，定期提交
3. **接口约定**: 提前定义好信号和函数接口
4. **测试验证**: 每完成一个模块就进行联合测试

---

## 🚀 快速开始

### 环境准备
1. 下载并安装 Godot 4.5.1
2. 克隆项目到本地
3. 打开 `project.godot` 文件

### 运行步骤
1. 按照Day 1计划创建基础场景
2. 逐日完成开发任务
3. 每天进行功能测试
4. Day 9完成最终版本

### 调试技巧
- 使用 `print()` 输出调试信息
- 利用Godot的远程调试功能
- 检查信号连接是否正确
- 监控节点树结构

---

## 📞 技术支持

### 常见问题
1. **场景找不到**: 检查文件路径是否正确
2. **信号不触发**: 确认信号名称和连接
3. **音效不播放**: 检查AudioManager设置
4. **性能问题**: 优化粒子效果和碰撞检测

### 学习资源
- [Godot官方文档](https://docs.godotengine.org/)
- [Godot中文教程](https://godot.cn/)
- [GDScript语法参考](https://docs.godotengine.org/en/stable/tutorials/scripting/gdscript/gdscript_basics.html)

---

**祝你们开发顺利！这个项目不仅有技术挑战，更有深刻的社会意义。🌟**
